import { async, ComponentFixture, TestBed } from '@angular/core/testing';
/*
describe('Studyfieldsresponse.ListItemComponent', () => {
  let component: Studyfieldsresponse.ListItemComponent;
  let fixture: ComponentFixture<Studyfieldsresponse.ListItemComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ Studyfieldsresponse.ListItemComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(Studyfieldsresponse.ListItemComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
